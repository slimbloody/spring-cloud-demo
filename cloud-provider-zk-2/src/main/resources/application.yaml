server:
  port: 8002

spring:
  application:
    name: cloud-provider-zk
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloud_test?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: yuri
    password: 123456
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
  cloud:
    zookeeper:
      connect-string:


mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: slimcomp.entity # 所有Entity别名类所在包
